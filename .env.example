# Database configuration for local development
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=flrsp
# Server
PORT=3000

# Database (choose mysql, sqlite, or sqljs)
DB_DISABLED=0
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=flrsp

# SQLite dev options
DB_SQLITE_PATH=dev.sqlite
DB_SYNC=0

# Sql.js dev options (no native build, recommended on Windows)
# Set DB_TYPE=sqljs to use these
DB_SQLJS_PATH=dev.sqljs

# Auth
AUTH_NO_DB=0
JWT_SECRET=change_me_dev_secret